# Copy to `.env` for local runs (do not commit).

PATH="$HOME/.local/bin:$PATH"

GREENBONE_CONNECTION=socket
GREENBONE_SOCKET_PATH=/home/brahim/gvm-sockets/gvmd/gvmd.sock
GREENBONE_GMP_USERNAME=admin
GREENBONE_GMP_PASSWORD=change-me
GREENBONE_SSH_CREDENTIAL_ID=change-me
GREENBONE_SSH_PORT=22

OPENSEARCH_HOST=localhost
OPENSEARCH_PORT=9200
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=admin

DEFECTDOJO_URL=http://localhost:8080
DEFECTDOJO_TOKEN=change-me
